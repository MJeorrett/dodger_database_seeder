<% @current_path = :seeds %>
<% @current_route = :show %>

<%= erb(:'partials/breadcrumb') %>

<hr>

<%= erb(:'seeds/partials/seed_details') %>

<hr>

<% form_action = "/databases/#{@db_name}/tables/#{@table_name}/seeds/#{@seed.id}" %>
<form action="<%= form_action %>" method="post">
  <input class="button" type="submit" value="Delete Seed" />
  <input type="hidden" name="_method" value="delete" />
</form>
